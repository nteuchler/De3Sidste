services:
  - type: web
    name: de3sidste
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash -lc 'gunicorn --bind 0.0.0.0:${PORT} app:app'
    autoDeploy: true
